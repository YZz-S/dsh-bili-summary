# 复制本文件为 .env 后按需填写；.env 已被 .gitignore 忽略，不会进入版本库。
#
# B站登录 Cookie（可选）：出现风控(-412)/需登录(-352)时启用。
# 插件形态：填进 preset 行的 config.cookie（见 examples/cordis.row.yml）；
# 技能形态：按 SKILL.md 配置节的 {COOKIE} 约定使用。
# 示例（请替换为真实值）：
# BILI_COOKIE=SESSDATA=xxxxxxxx; bili_jct=xxxxxxxx
