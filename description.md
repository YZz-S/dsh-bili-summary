# bili-summary — 项目描述

## 一句话简介 / Tagline

> DeepSeek Harness Host 插件：`bili_summary` 工具，一次调用取回 B站视频的元数据、字幕时间轴与 sharp 切帧配图。
> DeepSeek Harness Host plugin: the `bili_summary` tool — Bilibili metadata, subtitle timeline and sharp frame cutting in one call.

## Description（GitHub About 可直接粘贴）

**中文**：

```
DeepSeek Harness Host 插件：bili_summary 工具——B站视频元数据、字幕时间轴与 sharp 切帧配图，错误处理与降级内置，跨平台纯 Node 实现
```

**English**:

```
DeepSeek Harness Host plugin: the bili_summary tool — Bilibili video metadata, subtitle timeline and sharp-based frame cutting, with built-in error handling and graceful degradation
```

## Topics（推荐）

```
bilibili  bilibili-api  deepseek-harness  cordis  ai-agents  llm-tools  video-summary  subtitle  sharp  markdown  nodejs
```

选择理由：

| Topic | 理由 |
| --- | --- |
| `bilibili` / `bilibili-api` | 目标平台与接口生态，最大发现流量 |
| `deepseek-harness` / `cordis` | 运行时与插件框架，DSH 用户搜索必经词 |
| `ai-agents` / `llm-tools` | Agent 工具生态通用词，跨框架可见性 |
| `video-summary` / `subtitle` | 功能关键词，精确匹配使用场景 |
| `sharp` | 切帧实现依赖，sharp 生态用户可见 |
| `markdown` | 产物格式（结构化笔记） |
| `nodejs` | 语言，覆盖语言类检索 |
